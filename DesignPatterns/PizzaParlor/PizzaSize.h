#ifndef PIZZA_SIZE_H
#define PIZZA_SIZE_H

#include <string>
#include <vector>
#include <list>
#include <iostream>

/** @enum       PizzaSize
    @details    this enum holds the different possible pizza sizes
*/
enum PizzaSize
{
    INDIVIDUAL,
    REGULAR,
    FAMILY
};

#endif
